---
// Testimonials are intentionally absent until real client permission exists.
// When ready: fill the array below, then uncomment the section markup and
// drop <Testimonials /> into src/pages/index.astro after the services grid.
//
// const testimonials = [
//   {
//     quote: '',
//     name: '',
//     role: '',
//     company: '',
//   },
// ];
---

<!--
<section class="bg-ivory">
  <div class="container-m section">
    <p class="eyebrow">What clients say</p>
    <div class="mt-8 grid gap-8 md:grid-cols-2">
      {testimonials.map((t) => (
        <figure class="border border-line bg-white p-8">
          <blockquote class="font-serif text-xl leading-snug tracking-tight text-navy">
            {t.quote}
          </blockquote>
          <figcaption class="mt-6 text-sm">
            <span class="font-semibold text-navy">{t.name}</span>, {t.role}, {t.company}
          </figcaption>
        </figure>
      ))}
    </div>
  </div>
</section>
-->
